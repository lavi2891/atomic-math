# Set to the versioned Apps Script /exec URL for a classroom build.
# Leave blank only for intentional offline/local development.
VITE_APPS_SCRIPT_URL=

# Optional explicit development/test fallback (for example LOCAL-DEMO).
# Leave blank for classroom builds so learners enter their own runtime code.
VITE_STUDENT_ID=
